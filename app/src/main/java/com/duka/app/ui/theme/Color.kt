package com.duka.app.ui.theme

import androidx.compose.ui.graphics.Color

// Duka Design System Colors — match spec exactly
val Ink = Color(0xFF1C2321)
val Canvas = Color(0xFFF3EFE6)
val Forest = Color(0xFF1F4B3F)
val ForestLight = Color(0xFF2C6350)
val Amber = Color(0xFFD9A441)
val Clay = Color(0xFF8B3A2B)
val Mist = Color(0xFFE4DFD3)
val White = Color(0xFFFFFFFF)

// Semantic aliases
val Primary = Forest
val Secondary = ForestLight
val Accent = Amber
val Warning = Clay
val Background = Canvas
val Surface = Mist
val OnPrimary = White
val OnBackground = Ink
val OnSurface = Ink
val OnSurfaceVariant = Color(0xFF5A5A5A)

// Status chip colors
val StatusActive = Forest
val StatusPending = Amber
val StatusWarning = Clay
